title: "CRITICA ALGORITHMI"
description: "Critical Scrutiny of the Applications and Implications of Algorithms in the Humanities"
remote_theme: pages-themes/minimal@v0.2.0
plugins:
- jekyll-remote-theme

permissions:
  contents: read
  pages: write
  id-token: write

# name: Deploy to Pages
#   id: deployment
#   uses: actions/deploy-pages@v4
#   continue-on-error: true

# name: Retry deployment
#   if: failure()
#   run: sleep 30 && echo "Retrying deployment..."
